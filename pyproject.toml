[project]
name = "rubin-sizing-model"
version = "2026.09"
description = "Compute, storage and cost model for Rubin Observatory USDF operations, FY2026-FY2035"
requires-python = ">=3.10"
dependencies = [
    "openpyxl>=3.1",
    "pyyaml>=6.0",
]

[tool.uv]
package = false
